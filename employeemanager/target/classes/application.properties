# URL of the MySQL database
#spring.datasource.url=jdbc:mysql://localhost:3306/pujadb

# MySQL username
#spring.datasource.username=root

# MySQL password
#spring.datasource.password=root

# Class name of the MySQL JDBC driver
#spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# Hibernate SQL dialect for MySQL
#spring.jpa.database-platform = org.hibernate.dialect.MySQLDialect

# Enable Hibernate DDL generation (create, drop, etc.)
#spring.jpa.generate-ddl=true

# Hibernate DDL auto (create, create-drop, update)
#spring.jpa.hibernate.ddl-auto =create

spring.datasource.url= jdbc:mysql://localhost:8080/pujadb

    spring.datasource.username=root

    spring.datasource.password=root

    spring.jpa.hibernate.ddl-auto=create-drop
    spring.jpa.show-sql=true
    spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLInnoDBDialect